# 작업 관리 — UI 화면 스펙

> 최종 수정일: 2026-03-03

---

## 1. 화면 개요

| 항목 | 내용 |
|------|------|
| **화면 ID** | `owner-order-manage` |
| **화면 명** | 작업 관리 |
| **목적** | 전체 작업 목록을 한눈에 확인하고, 원탭으로 빠르게 작업 상태를 변경하여 효율적으로 작업을 관리한다 |
| **사용자 역할** | shop_owner |
| **진입 조건** | 로그인 필수, role이 `shop_owner`, 샵 등록 완료 상태 |

---

## 2. 레이아웃 구조

```
┌─────────────────────────────┐
│  상단 앱바                    │
│  "작업 관리"                  │
├─────────────────────────────┤
│  상태 필터 탭                 │
│  [전체] [접수됨] [작업중] [완료]│
├─────────────────────────────┤
│                             │
│  작업 목록 (스크롤 영역)       │
│  ┌───────────────────────┐  │
│  │ 홍길동         ● 접수됨│  │
│  │ 14:30         [작업시작]│  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 김철수         ● 작업중│  │
│  │ 13:00         [작업완료]│  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 이영희          ✓ 완료 │  │
│  │ 10:30                 │  │
│  └───────────────────────┘  │
│          ...                │
│                             │
├─────────────────────────────┤
│  하단 네비게이션 바            │
│  대시보드|작업관리|설정        │
└─────────────────────────────┘
```

### 섹션 설명

| 섹션 | 설명 | 비고 |
|------|------|------|
| 상단 앱바 | "작업 관리" 타이틀 | 배경 `#FFFFFF` |
| 상태 필터 탭 | 전체/접수됨/작업중/완료 4개 탭으로 필터링 | 수평 스크롤 없이 고정 배치 |
| 작업 목록 | 필터에 맞는 작업을 시간순으로 표시 | 세로 스크롤, Pull-to-refresh 지원 |
| 하단 네비게이션 | 3개 탭 (작업관리 활성) | 활성 색상 `#2563EB` |

---

## 3. 컴포넌트 목록

### 3.1 상단 앱바

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| 타이틀 | Text | "작업 관리" (`titleLarge`, 18sp, SemiBold) | 항상 표시 |

### 3.2 상태 필터 탭

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| 전체 탭 | FilterChip | "전체 (N)" | 기본 선택 |
| 접수됨 탭 | FilterChip | "접수됨 (N)" | 선택 시 접수됨만 필터 |
| 작업중 탭 | FilterChip | "작업중 (N)" | 선택 시 작업중만 필터 |
| 완료 탭 | FilterChip | "완료 (N)" | 선택 시 완료만 필터 |

**필터 탭 스타일:**
- 선택됨: 배경 `#2563EB`, 텍스트 `#FFFFFF`, 모서리 `radius_full` (999px)
- 미선택: 배경 `#EFF6FF`, 텍스트 `#4A4A5A`, 모서리 `radius_full` (999px)
- 높이: 36px
- 탭 간 간격: 8px
- 수평 패딩: 16px, 수직 패딩: 8px
- 각 탭에 해당 상태의 건수를 괄호 안에 표시

### 3.3 작업 카드 (리스트 아이템)

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| 카드 컨테이너 | Card | 작업 한 건의 전체 영역 | 탭 시 상세 바텀시트 |
| 회원 이름 | Text | `titleMedium` (16sp, Medium) | 항상 표시 |
| 상태 뱃지 | StatusBadge | 접수됨/작업중/완료 | 카드 우측 상단 |
| 접수 시간 | Text | "14:30" 또는 "어제 14:30" (`bodySmall`, 12sp, `#9CA3AF`) | 카드 좌측 하단 |
| 메모 아이콘 | Icon | `note` 아이콘 (12px, `#9CA3AF`) | 메모가 있을 때만 표시 |
| 상태 변경 버튼 | FilledButton (compact) | 다음 상태로 변경 | 완료 상태이면 숨김 |

**작업 카드 스타일:**
- 배경: `#FFFFFF`
- 모서리: 20px
- 내부 패딩: 16px
- 카드 간 간격: 8px
- 테두리: `#E8E0D8` 1px
- 그림자: elevation 0 (플랫한 느낌)

**상태 변경 버튼 (카드 내부):**

| 현재 상태 | 버튼 텍스트 | 버튼 배경 | 버튼 크기 |
|-----------|------------|----------|----------|
| 접수됨 | "작업 시작" | `#2563EB` | 높이 32px, 패딩 수평 12px |
| 작업중 | "작업 완료" | `#2563EB` | 높이 32px, 패딩 수평 12px |
| 완료 | (숨김) | - | - |

- 버튼 텍스트: `#FFFFFF`, `labelMedium` (12sp, Medium)
- 모서리: 14px
- 카드 우측 하단에 배치

### 3.4 하단 네비게이션

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| 대시보드 탭 | BottomNavItem | `dashboard` + "대시보드" | 비활성 (`#9CA3AF`) |
| 작업관리 탭 | BottomNavItem | `assignment` + "작업관리" | 활성 (`#2563EB`) |
| 설정 탭 | BottomNavItem | `settings` + "설정" | 비활성 (`#9CA3AF`) |

**아이콘 폰트:** Material Symbols Rounded / **라벨:** 10sp / **아이콘-라벨 간격:** 4px / **높이:** 80px

---

## 4. 필드 정의

이 화면은 입력 필드가 없는 조회/관리 화면이다. 표시되는 데이터 필드는 아래와 같다.

| 필드명 | 타입 | 데이터 출처 | 표시 형식 | 설명 |
|--------|------|-------------|-----------|------|
| member_name | String | members.name | 텍스트 | 회원 이름 |
| status | Enum | orders.status | StatusBadge | 작업 상태 |
| memo | String | orders.memo | 메모 아이콘 (있을 때) | 작업 메모 |
| created_at | Timestamp | orders.created_at | "HH:mm" 또는 "M/D HH:mm" | 접수 시간 |

---

## 5. 사용자 인터랙션

| # | 이벤트 | 트리거 | 액션 | 결과 |
|---|--------|--------|------|------|
| 1 | 필터 탭 변경 | 상태 필터 탭 터치 | 선택된 상태로 목록 필터링 | 목록이 해당 상태의 작업만 표시, 애니메이션 전환 |
| 2 | 상태 변경 (원탭) | 작업 카드 내 "작업 시작" 또는 "작업 완료" 버튼 탭 | 즉시 상태 변경 API 호출 | 뱃지/버튼 즉시 갱신, "실행 취소" 스낵바 3초, 고객 푸시 알림 |
| 3 | 작업 카드 탭 | 카드 본문 영역(버튼 제외) 터치 | 작업 상세 바텀시트 표시 | 전체 작업 정보 + 회원 정보 + 메모 + 상태 변경 옵션 |
| 4 | 아래로 당기기 | Pull-to-refresh | 작업 목록 재조회 | 로딩 인디케이터 후 목록 갱신 |
| 5 | 스와이프 삭제 | 카드 좌측 스와이프 | 삭제 확인 다이얼로그 표시 | "이 작업을 삭제하시겠습니까?" 확인/취소 |
| 6 | 하단 탭 전환 | 하단 네비게이션 탭 터치 | 해당 화면으로 이동 | 선택된 탭 화면 전환 |

---

## 6. API 연동

### 6.1 데이터 조회

| API | 테이블 | 조건 | 반환 데이터 | 호출 시점 |
|-----|--------|------|-------------|-----------|
| 작업 목록 조회 | `orders` JOIN `members` | `shop_id = 내_샵_id` ORDER BY `created_at` DESC | order.*, member.name, member.phone | 화면 진입 시, Pull-to-refresh 시, 필터 변경 시 |
| 상태별 건수 조회 | `orders` | `shop_id = 내_샵_id` | COUNT by status | 화면 진입 시 (필터 탭 건수 표시용) |

**Supabase 쿼리 예시:**

```dart
// 작업 목록 조회 (필터 적용)
var query = supabase
  .from('orders')
  .select('*, members(name, phone)')
  .eq('shop_id', myShopId)
  .order('created_at', ascending: false);

if (statusFilter != null) {
  query = query.eq('status', statusFilter);
}

final orders = await query.limit(50);
```

### 6.2 데이터 변경

| API | 테이블 | 동작 | 파라미터 | 호출 시점 |
|-----|--------|------|----------|-----------|
| 작업 상태 변경 | `orders` | UPDATE | `{status: 새_상태, updated_at: now()}` | 상태 변경 버튼 탭 시 |
| 작업 삭제 | `orders` | DELETE | `{id: order_id}` | 스와이프 삭제 확인 시 |

### 6.3 실시간 구독

| 구독 대상 | 테이블 | 조건 | 이벤트 | 처리 |
|-----------|--------|------|--------|------|
| 내 샵 작업 변경 | `orders` | `shop_id = 내_샵_id` | INSERT, UPDATE, DELETE | 목록 갱신, 필터 탭 건수 재계산 |

---

## 7. 상태 관리

### 7.1 화면 상태

| 상태 | 조건 | UI 표시 |
|------|------|---------|
| **로딩** | 최초 데이터 로드 중 | 스켈레톤 shimmer 카드 5개 표시 |
| **빈 상태 (전체)** | 작업이 0건 | 일러스트 + "등록된 작업이 없습니다" + "작업 접수 버튼으로 새 작업을 등록하세요" |
| **빈 상태 (필터)** | 선택된 필터에 해당하는 작업 0건 | "해당 상태의 작업이 없습니다" |
| **에러** | 네트워크/서버 오류 | "데이터를 불러올 수 없습니다" + 재시도 버튼 |
| **정상** | 작업 1건 이상 | 필터 탭 + 작업 목록 표시 |

### 7.2 로컬 상태 변수

| 변수명 | 타입 | 초기값 | 설명 |
|--------|------|--------|------|
| `isLoading` | bool | true | 데이터 로딩 중 여부 |
| `orders` | List\<Order\> | [] | 전체 작업 목록 |
| `selectedFilter` | String? | null | 선택된 상태 필터 (null = 전체) |
| `statusCounts` | Map\<String, int\> | {} | 상태별 건수 |
| `hasError` | bool | false | 에러 발생 여부 |

---

## 8. 네비게이션

### 8.1 진입 경로

| 출발 화면 | 동작 | 전달 파라미터 |
|-----------|------|---------------|
| 하단 네비게이션 | "작업관리" 탭 터치 | 없음 |
| 사장님 대시보드 | "전체보기" 링크 또는 카운트 카드 탭 | `statusFilter` (선택적) |

### 8.2 이동 가능 화면

| 대상 화면 | 동작 | 전달 파라미터 |
|-----------|------|---------------|
| 작업 상세 바텀시트 | 작업 카드 탭 | `order_id` |
| 대시보드 (`owner-dashboard`) | 하단 "대시보드" 탭 | 없음 |
| 설정 (`owner-shop-settings`) | 하단 "설정" 탭 | 없음 |

---

## 9. 비고

- **원탭 상태 변경**: 카드 내 상태 변경 버튼을 탭하면 확인 다이얼로그 없이 즉시 변경한다. 이는 바쁜 샵 운영 환경에서 빠른 조작을 위한 설계다. 오조작 방지를 위해 3초간 "실행 취소" 스낵바를 표시한다.
- **작업 상세 바텀시트**: 카드 탭 시 바텀시트로 상세 정보를 표시한다. 내용: 회원 정보(이름, 연락처), 메모, 접수 시간, 상태 타임라인, 상태 변경 버튼. 바텀시트 상단 모서리: 16px.
- **날짜 구분**: 작업 목록은 날짜별로 그룹핑하여 표시한다. "오늘", "어제", "2026-02-18" 식으로 날짜 헤더를 삽입한다.
- **무한 스크롤**: 최초 50건 로드 후 하단 스크롤 시 추가 50건씩 로드한다 (pagination).
- **대시보드에서 진입 시**: `statusFilter`가 전달되면 해당 필터 탭이 자동으로 선택된 상태로 표시한다.
- **카카오택시 참고**: 상태 변경 흐름을 카카오택시의 빠른 상태 전환 UX를 참고하여 설계. 최소한의 탭으로 상태 변경이 가능하도록 한다.
- **삭제 기능**: 스와이프 삭제는 `접수됨` 상태의 작업에만 허용한다. 작업중/완료 상태는 스와이프 비활성.
- **텍스트 명암비**: 본문 텍스트(`#4A4A5A`)와 배경(`#FFFFFF`)의 명암비는 WCAG AA 기준 4.5:1 이상을 충족해야 한다.
- **터치 타겟**: 모든 터치 가능 요소(필터 탭, 작업 카드, 상태 변경 버튼)의 터치 영역은 최소 44x44px을 보장한다. 상태 변경 버튼(높이 32px)은 터치 영역을 패딩으로 확장하여 44px을 보장한다. 인접 터치 타겟 간 최소 8px 간격을 유지한다.

---

## UX 가이드라인 체크리스트

> ui-ux-pro-max 기반 검증

### 접근성 (CRITICAL)
- [x] 텍스트 명암비 4.5:1 이상
- [x] 아이콘 전용 버튼에 접근성 라벨 명시
- [x] 색상만으로 정보 구분하지 않음 (아이콘+텍스트 병행) (상태 뱃지에 텍스트 라벨 포함)

### 터치 & 인터랙션 (CRITICAL)
- [x] 터치 타겟 최소 44x44px
- [x] 인접 터치 타겟 간 최소 8px 간격 (카드 간 8px, 필터 탭 간 8px)
- [x] 비동기 버튼 로딩 상태 + 중복 클릭 방지 (Pull-to-refresh 로딩 인디케이터)
- [x] 에러 시 해당 필드 근처에 메시지 표시 (에러 상태: "데이터를 불러올 수 없습니다" + 재시도)
- [x] 파괴적 액션 확인 대화상자 (스와이프 삭제 시 확인 다이얼로그 "이 작업을 삭제하시겠습니까?")

### 레이아웃 (HIGH)
- [x] 모바일 본문 폰트 최소 16px (회원 이름 16sp, 타이틀 18sp)
- [x] 가로 스크롤 없음 (필터 탭 고정 배치, 세로 스크롤만 사용)
- [x] 고정 요소 아래 콘텐츠 패딩 확보 (필터 탭 고정, 하단 네비게이션 바 위 콘텐츠 패딩)

### 피드백 (HIGH)
- [x] 로딩 상태 정의 (스켈레톤/스피너) (스켈레톤 shimmer 카드 5개)
- [x] 빈 상태(Empty State) UI 정의 (전체 빈 상태 + 필터별 빈 상태 모두 정의)
- [x] 성공/실패 피드백 정의 (상태 변경 후 "실행 취소" 스낵바, 에러 시 재시도)
- N/A 다단계 프로세스 진행 표시기 (단일 화면, 다단계 프로세스 없음)

### 폼 (해당 시)
- N/A 모든 입력 필드에 라벨 명시 (입력 필드 없는 조회/관리 화면)
- N/A 필수 필드 표시 (*)
- N/A 입력 타입별 적절한 키보드
- N/A 비밀번호 표시/숨기기 토글

### 일러스트레이션 & 애니메이션 구현 참고

> 구현 시 아래 사항을 반영할 것. Pencil 디자인에서는 정적 표현만 가능하므로 여기에 동적 요소를 명시한다.

| 요소 | 현재 (정적) | 구현 목표 (동적) | 구현 방법 |
|------|------------|-----------------|-----------|
| 빈 상태 일러스트 | 텍스트만 | Lottie 애니메이션 | `lottie` 패키지. 빈 클립보드 위 셔틀콕. `assets/animations/empty_manage.json` |
| 필터 탭 전환 | 즉시 전환 | 목록 페이드 전환 | `AnimatedSwitcher(duration: 200ms)` 로 필터 변경 시 목록 교체 |
| 상태 변경 | 즉시 반영 | 카드 색상 전환 300ms | `AnimatedContainer(duration: 300ms)` 로 좌측 컬러바 색상 전환 |

**Lottie 에셋 준비:**
- 파일: `assets/animations/empty_manage.json`
- 테마: 빈 작업 목록 — 클립보드 + 셔틀콕
- 색상: `#9CA3AF` (tertiary) + `#E8E0D8` (border)
- 크기: 100x100px
- 재생: 무한 루프 (loop: true)
- 대안: Lottie 에셋 준비 전까지 텍스트 기반 빈 상태 유지

### Flutter 구현 참고
- [x] StatelessWidget vs StatefulWidget 구분 (실시간 구독 + 필터 상태 → StatefulWidget 필요)
- [x] ListView.builder 사용 (목록 화면) (무한 스크롤 50건씩 로드, ListView.builder 적합)
- N/A Form + GlobalKey 유효성 검증 (폼 화면) (폼 없음)
