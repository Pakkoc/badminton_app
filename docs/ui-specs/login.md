# 로그인 — UI 화면 스펙

> 최종 수정일: 2026-03-12

---

## 1. 화면 개요

| 항목 | 내용 |
|------|------|
| **화면 ID** | `login` |
| **화면 명** | 로그인 |
| **목적** | 소셜 로그인(카카오, 네이버, Gmail)으로 간편하게 로그인/가입한다. 신규 사용자는 프로필 설정 화면으로 이동한다 |
| **사용자 역할** | 공통 |
| **진입 조건** | 미로그인 상태 (Supabase Auth 세션 없음) |

---

## 2. 레이아웃 구조

```
┌─────────────────────────────┐
│                             │
│        앱 로고 영역           │
│      셔틀콕 아이콘            │
│        "거트알림"             │
│                             │
├─────────────────────────────┤
│                             │
│       반갑습니다!             │
│     간편하게 시작하세요         │
│                             │
├─────────────────────────────┤
│                             │
│  ┌───────────────────────┐  │
│  │   카카오로 시작하기      │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │  N  네이버로 시작하기   │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │  G  Gmail로 시작하기   │  │
│  └───────────────────────┘  │
│                             │
└─────────────────────────────┘
```

### 섹션 설명

| 섹션 | 설명 | 비고 |
|------|------|------|
| 앱 로고 영역 | 상단에 셔틀콕 아이콘 + "거트알림" 텍스트 배치 | 스플래시보다 작은 크기, 화면 상단에서 80px 아래 |
| 환영 문구 영역 | "반갑습니다!" + "간편하게 시작하세요" 텍스트 | 로고 아래 40px 간격 |
| 소셜 로그인 버튼 영역 | 카카오, 네이버, Gmail 소셜 로그인 버튼 3개 | 환영 문구 아래 40px 간격, 버튼 사이 12px 간격 |

---

## 3. 컴포넌트 목록

### 3.1 앱 로고

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| 셔틀콕 아이콘 | Icon/Image | 배드민턴 셔틀콕 아이콘 | 항상 표시 |
| 앱 이름 텍스트 | Text | "거트알림" | 항상 표시 |

#### 로고 스타일

| 속성 | 값 |
|------|-----|
| 아이콘 크기 | 56 x 56px |
| 아이콘 색상 | `#F59E0B` (Amber) |
| 앱 이름 크기 | 28sp (`displayMedium`) |
| 앱 이름 굵기 | Bold (700) |
| 앱 이름 색상 | `#FFFFFFEE` (Text Primary, White 93%) |
| 아이콘-텍스트 간격 | 12px |
| 정렬 | 가로 중앙 |

### 3.2 환영 문구

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| 메인 문구 | Text | "반갑습니다!" | 항상 표시 |
| 서브 문구 | Text | "간편하게 시작하세요" | 항상 표시 |

#### 환영 문구 스타일

| 속성 | 값 |
|------|-----|
| 메인 문구 텍스트 크기 | 22sp (`headlineSmall`) |
| 메인 문구 굵기 | Bold (700) |
| 메인 문구 색상 | `#FFFFFFEE` (Text Primary, White 93%) |
| 서브 문구 텍스트 크기 | 16sp (`bodyLarge`) |
| 서브 문구 굵기 | Regular (400) |
| 서브 문구 색상 | `#FFFFFFCC` (Text Secondary, White 80%) |
| 문구 간 간격 | 8px |
| 정렬 | 가로 중앙 |

### 3.3 소셜 로그인 버튼

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| 카카오 로그인 버튼 | Button | 카카오 계정으로 로그인/가입 | 항상 활성, 로딩 중이면 스피너 표시 |
| 네이버 로그인 버튼 | Button | 네이버 계정으로 로그인/가입 | 항상 활성, 로딩 중이면 스피너 표시 |
| Gmail 로그인 버튼 | Button | Gmail 계정으로 로그인/가입 | 항상 활성, 로딩 중이면 스피너 표시 |

#### 카카오 로그인 버튼 스타일

| 속성 | 값 |
|------|-----|
| 너비 | 화면 전체 (수평 패딩 16px 제외) |
| 높이 | 48px |
| 배경색 | `#FEE500` (Kakao Yellow) |
| 텍스트 | "카카오로 시작하기" |
| 텍스트 색상 | `#191919` |
| 텍스트 크기 | 16sp (`titleMedium`) |
| 텍스트 굵기 | SemiBold (600) |
| 모서리 둥글기 | 20px (`radius_lg`) |
| 좌측 아이콘 | 카카오톡 말풍선 아이콘, 색상 `#191919` |

#### 네이버 로그인 버튼 스타일

| 속성 | 값 |
|------|-----|
| 너비 | 화면 전체 (수평 패딩 16px 제외) |
| 높이 | 48px |
| 배경색 | `#03C75A` (Naver Green) |
| 텍스트 | "N  네이버로 시작하기" |
| 텍스트 색상 | `#FFFFFF` |
| 텍스트 크기 | 16sp (`titleMedium`) |
| 텍스트 굵기 | SemiBold (600) |
| 모서리 둥글기 | 20px (`radius_lg`) |
| 좌측 아이콘 | "N" 텍스트 아이콘, 색상 `#FFFFFF` |

#### Gmail 로그인 버튼 스타일

| 속성 | 값 |
|------|-----|
| 너비 | 화면 전체 (수평 패딩 16px 제외) |
| 높이 | 48px |
| 배경색 | `#FFFFFF18` (surfaceHigh) |
| 테두리 | `#FFFFFF20` 1.5px |
| 텍스트 | "G  Gmail로 시작하기" |
| 텍스트 색상 | `#FFFFFFEE` |
| 텍스트 크기 | 16sp (`titleMedium`) |
| 텍스트 굵기 | SemiBold (600) |
| 모서리 둥글기 | 20px (`radius_lg`) |
| 좌측 아이콘 | Google "G" 로고, 색상 멀티컬러 |

#### 버튼 간격

| 속성 | 값 |
|------|-----|
| 버튼 사이 간격 | 12px |

---

## 4. 필드 정의

입력 필드 없음. 소셜 로그인 방식으로 전환되어 사용자가 직접 입력해야 하는 폼 필드가 존재하지 않는다.

---

## 5. 사용자 인터랙션

| # | 이벤트 | 트리거 | 액션 | 결과 |
|---|--------|--------|------|------|
| 1 | 카카오 로그인 버튼 탭 | 카카오 로그인 버튼 터치 | Supabase Auth `signInWithOAuth(OAuthProvider.kakao)` 호출 | 카카오 인증 페이지로 이동 |
| 2 | 네이버 로그인 버튼 탭 | 네이버 로그인 버튼 터치 | Supabase Auth 커스텀 프로바이더(Naver) 호출 | 네이버 인증 페이지로 이동 |
| 3 | Gmail 로그인 버튼 탭 | Gmail 로그인 버튼 터치 | Supabase Auth `signInWithOAuth(OAuthProvider.google)` 호출 | Google 인증 페이지로 이동 |

### 소셜 로그인 처리 흐름

```
소셜 로그인 버튼 탭
  ↓
버튼 로딩 상태로 전환 (스피너 표시)
  ↓
Supabase Auth signInWithOAuth 호출
  ↓
소셜 플랫폼 인증 진행
  ↓
├── 인증 성공
│     ↓
│   users 테이블에서 사용자 존재 여부 및 role 조회
│     ↓
│   ├── 사용자 존재 (기존 사용자)
│   │     ├── role = "customer" → 고객 홈으로 이동
│   │     └── role = "shop_owner" → 사장님 대시보드로 이동
│   │
│   └── 사용자 없음 (신규 사용자) → 프로필 설정 화면으로 이동
│
└── 인증 실패
      ↓
    버튼 로딩 해제
      ↓
    에러 타입에 따른 메시지 표시
        ├── 사용자 취소 → 별도 메시지 없음 (기본 상태 복원)
        ├── 네트워크 오류 → "네트워크 연결을 확인해주세요"
        └── 기타 오류 → "로그인에 실패했습니다. 다시 시도해주세요"
```

### 에러 스낵바 스타일

| 속성 | 값 |
|------|-----|
| 배경색 | `#EF444426` (Error Background) |
| 텍스트 색상 | `#EF4444` (Error) |
| 좌측 아이콘 | `error_outline`, 색상 `#EF4444` |
| 모서리 둥글기 | 8px |
| 표시 위치 | 화면 상단 (앱바 아래) |
| 표시 시간 | 3초 후 자동 사라짐 |

---

## 6. API 연동

### 6.1 데이터 조회

이 화면에서 초기 데이터 조회는 발생하지 않는다.

### 6.2 데이터 변경

| API | 테이블 | 동작 | 파라미터 | 호출 시점 |
|-----|--------|------|----------|-----------|
| 카카오 로그인 | Supabase Auth | `signInWithOAuth` | `OAuthProvider.kakao` | 카카오 버튼 탭 시 |
| Google 로그인 | Supabase Auth | `signInWithOAuth` | `OAuthProvider.google` | Gmail 버튼 탭 시 |
| 네이버 로그인 | Edge Function | 브라우저 OAuth → `naver-auth` EF | 네이버 버튼 탭 시 (url_launcher → EF → 네이버 OAuth → 딥링크 콜백) |
| 사용자 조회 | `users` | SELECT | `id = auth.uid` | 소셜 로그인 성공 후 |

**Supabase 쿼리 예시:**

```dart
// 1. 카카오 로그인
await supabase.auth.signInWithOAuth(
  OAuthProvider.kakao,
  redirectTo: 'com.gurtalim.app://login-callback',
);

// 2. Google 로그인
await supabase.auth.signInWithOAuth(
  OAuthProvider.google,
  redirectTo: 'com.gurtalim.app://login-callback',
);

// 3. 네이버 로그인 (Edge Function 브라우저 OAuth)
// url_launcher로 EF URL 오픈 → 네이버 OAuth → 딥링크로 토큰 수신
// AppLinks로 딥링크 수신 후 setSession(refreshToken)으로 세션 설정
await authRepository.signInWithNaver();

// 4. 인증 성공 후 사용자 존재 여부 확인
final userId = supabase.auth.currentUser!.id;
final result = await supabase
  .from('users')
  .select('role')
  .eq('id', userId)
  .maybeSingle();

// 5. 역할별 라우팅
if (result == null) {
  // 신규 사용자 → 프로필 설정 화면으로 이동
} else if (result['role'] == 'customer') {
  // 고객 홈으로 이동
} else if (result['role'] == 'shop_owner') {
  // 사장님 대시보드로 이동
}
```

### 6.3 실시간 구독

이 화면에서 실시간 구독은 사용하지 않는다.

---

## 7. 상태 관리

### 7.1 화면 상태

| 상태 | 조건 | UI 표시 |
|------|------|---------|
| **기본** | 화면 진입 시 | 소셜 로그인 버튼 3개 활성 상태 |
| **로딩** | 소셜 로그인 API 호출 중 | 탭한 버튼에 CircularProgressIndicator 표시, 모든 버튼 비활성 |
| **에러** | 로그인 실패 | 에러 스낵바 표시 + 버튼 기본 상태 복원 |

### 7.2 로컬 상태 변수

| 변수명 | 타입 | 초기값 | 설명 |
|--------|------|--------|------|
| `isLoading` | bool | false | 소셜 로그인 API 호출 중 여부 |

---

## 8. 네비게이션

### 8.1 진입 경로

| 출발 화면 | 동작 | 전달 파라미터 |
|-----------|------|---------------|
| 스플래시 (`splash`) | 미로그인 상태에서 자동 이동 | 없음 |
| 마이페이지 (`customer-mypage`) | 로그아웃 후 로그인 화면으로 이동 | 없음 |

### 8.2 이동 가능 화면

| 대상 화면 | 동작 | 전달 파라미터 |
|-----------|------|---------------|
| 고객 홈 (`customer-home`) | 소셜 로그인 성공 + 기존 사용자 + role이 `customer` | 없음 |
| 사장님 대시보드 (`owner-dashboard`) | 소셜 로그인 성공 + 기존 사용자 + role이 `shop_owner` | 없음 |
| 프로필 설정 (`profile-setup`) | 소셜 로그인 성공 + 신규 사용자 (users 테이블에 없음) | 없음 |

---

## 9. 비고

- 로그인 화면에서 **뒤로가기 (시스템 백 버튼)** 를 누르면 앱을 종료한다 (스플래시로 돌아가지 않음).
- 소셜 로그인 중 하나의 버튼을 탭하면 나머지 버튼도 함께 비활성화하여 중복 요청을 방지한다.
- OAuth 콜백 처리를 위해 딥링크 스킴(`redirectTo`)을 반드시 설정한다.
- 네이버 로그인은 Edge Function(`naver-auth`) 기반 브라우저 OAuth로 구현하며, 네이버 개발자 센터에 PC 웹 서비스 환경 + Callback URL 등록이 필요하다.
- 카카오 로그인은 카카오 개발자 센터에서 플랫폼 등록(Android 패키지명 + iOS 번들 ID) 및 동의 항목 설정이 필요하다.
- Gmail(Google) 로그인은 Google Cloud Console에서 OAuth 2.0 클라이언트 ID 발급이 필요하다.

### 접근성

- 소셜 로그인 버튼에 각각 `semanticLabel`을 명시한다: `"카카오로 시작하기"`, `"네이버로 시작하기"`, `"Gmail로 시작하기"`.
- 로딩 상태의 CircularProgressIndicator에 `semanticLabel: "로그인 중"` 을 명시한다.
- 텍스트 색상(`#FFFFFFEE`)과 배경(`#162E14`)의 명암비는 12.8:1 로 WCAG AA 기준(4.5:1)을 충족한다.
- 카카오 버튼 텍스트(`#191919`)와 배경(`#FEE500`)의 명암비는 WCAG AA 기준을 충족한다.
- 네이버 버튼 텍스트(`#FFFFFF`)와 배경(`#03C75A`)의 명암비를 확인하여 AA 기준 미달 시 텍스트 굵기 또는 크기를 보정한다.

---

## UX 가이드라인 체크리스트

> ui-ux-pro-max 기반 검증

### 접근성 (CRITICAL)
- ✅ 텍스트 명암비 4.5:1 이상 — 본문 텍스트(#FFFFFFEE on #FFFFFF = 15.4:1) 충족
- ✅ 아이콘 전용 버튼에 접근성 라벨 명시 — 각 소셜 버튼에 semanticLabel 추가 (비고 섹션 참조)
- ✅ 색상만으로 정보 구분하지 않음 (아이콘+텍스트 병행) — 각 소셜 버튼에 브랜드 아이콘 + 텍스트 병행

### 터치 & 인터랙션 (CRITICAL)
- ✅ 터치 타겟 최소 44x44px — 소셜 로그인 버튼 높이 48px
- ✅ 인접 터치 타겟 간 최소 8px 간격 — 버튼 사이 간격 12px
- ✅ 비동기 버튼 로딩 상태 + 중복 클릭 방지 — 섹션 7.1에 로딩 상태 정의, 로딩 중 모든 버튼 비활성 처리
- ✅ 에러 시 스낵바로 메시지 표시 — 섹션 5 에러 스낵바 스타일 정의
- N/A 파괴적 액션 확인 대화상자 — 파괴적 액션 없음

### 레이아웃 (HIGH)
- ✅ 모바일 본문 폰트 최소 16px — 버튼 텍스트 16sp
- ✅ 가로 스크롤 없음 — 세로 단방향 레이아웃
- ✅ 콘텐츠 하단 여백 확보 — SafeArea 및 하단 패딩 고려

### 피드백 (HIGH)
- ✅ 로딩 상태 정의 (스피너) — 탭한 버튼 내 CircularProgressIndicator 표시
- N/A 빈 상태(Empty State) UI 정의 — 데이터 조회 화면 아님
- ✅ 성공/실패 피드백 정의 — 로그인 성공 시 화면 전환, 실패 시 에러 스낵바 (섹션 5)
- N/A 다단계 프로세스 진행 표시기 — 단일 화면, 다단계 아님

### Flutter 구현 참고
- ✅ StatelessWidget vs StatefulWidget 구분 — `isLoading` 상태 변수 존재 (섹션 7.2), StatefulWidget 사용 필요
- N/A ListView.builder 사용 (목록 화면) — 목록 없음
- N/A Form + GlobalKey 유효성 검증 — 입력 폼 없음, 소셜 로그인만 사용
