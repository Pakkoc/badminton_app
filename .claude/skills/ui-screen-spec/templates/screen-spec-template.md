# {화면 이름} — UI 화면 스펙

> 최종 수정일: YYYY-MM-DD

---

## 1. 화면 개요

| 항목 | 내용 |
|------|------|
| **화면 ID** | `{screen-id}` |
| **화면 명** | {화면 한글 이름} |
| **목적** | {이 화면이 해결하는 문제 또는 제공하는 가치} |
| **사용자 역할** | {customer / shop_owner / 공통} |
| **진입 조건** | {로그인 필요 여부, 특정 역할 제한 등} |

---

## 2. 레이아웃 구조

```
┌─────────────────────────────┐
│         상단 앱바            │
├─────────────────────────────┤
│                             │
│       메인 콘텐츠 영역        │
│                             │
├─────────────────────────────┤
│        하단 네비게이션        │
└─────────────────────────────┘
```

### 섹션 설명

| 섹션 | 설명 | 비고 |
|------|------|------|
| {섹션1} | {설명} | {비고} |
| {섹션2} | {설명} | {비고} |

---

## 3. 컴포넌트 목록

### 3.1 {컴포넌트 그룹명}

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| {이름} | {Button/Text/Card/List/...} | {설명} | {활성/비활성/숨김 조건} |

### 3.2 {컴포넌트 그룹명}

| 컴포넌트 | 타입 | 설명 | 상태 |
|----------|------|------|------|
| {이름} | {타입} | {설명} | {상태} |

---

## 4. 필드 정의

| 필드명 | 타입 | 필수 | 기본값 | 검증 규칙 | 설명 |
|--------|------|------|--------|-----------|------|
| {필드명} | {String/Number/Date/...} | {Y/N} | {기본값} | {규칙} | {설명} |

---

## 5. 사용자 인터랙션

| # | 이벤트 | 트리거 | 액션 | 결과 |
|---|--------|--------|------|------|
| 1 | {이벤트명} | {사용자 동작} | {시스템 동작} | {결과 상태/화면 변화} |
| 2 | {이벤트명} | {사용자 동작} | {시스템 동작} | {결과 상태/화면 변화} |

---

## 6. API 연동

### 6.1 데이터 조회

| API | 테이블 | 조건 | 반환 데이터 | 호출 시점 |
|-----|--------|------|-------------|-----------|
| {API명} | {테이블명} | {WHERE 조건} | {필드 목록} | {화면 진입 시/버튼 탭 시/...} |

### 6.2 데이터 변경

| API | 테이블 | 동작 | 파라미터 | 호출 시점 |
|-----|--------|------|----------|-----------|
| {API명} | {테이블명} | {INSERT/UPDATE/DELETE} | {파라미터} | {호출 시점} |

### 6.3 실시간 구독

| 구독 대상 | 테이블 | 조건 | 이벤트 | 처리 |
|-----------|--------|------|--------|------|
| {대상} | {테이블명} | {조건} | {INSERT/UPDATE/DELETE} | {UI 갱신 내용} |

---

## 7. 상태 관리

### 7.1 화면 상태

| 상태 | 조건 | UI 표시 |
|------|------|---------|
| **로딩** | {조건} | {로딩 인디케이터 설명} |
| **빈 상태** | {조건} | {빈 상태 메시지 및 안내} |
| **에러** | {조건} | {에러 메시지 및 재시도 버튼} |
| **정상** | {조건} | {정상 화면 설명} |

### 7.2 로컬 상태 변수

| 변수명 | 타입 | 초기값 | 설명 |
|--------|------|--------|------|
| {변수명} | {타입} | {초기값} | {설명} |

---

## 8. 네비게이션

### 8.1 진입 경로

| 출발 화면 | 동작 | 전달 파라미터 |
|-----------|------|---------------|
| {화면명} | {탭/버튼 클릭 등} | {파라미터} |

### 8.2 이동 가능 화면

| 대상 화면 | 동작 | 전달 파라미터 |
|-----------|------|---------------|
| {화면명} | {탭/버튼 클릭 등} | {파라미터} |

---

## 9. 비고

- {추가 고려사항, 디자인 참고사항, 엣지 케이스 등}
